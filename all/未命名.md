```mermaid
digraph ERD {
  graph [rankdir=LR, fontsize=12];
  node [shape=record, fontname="Helvetica"];

  PCAP_File [label="{<file_id> file_id (PK) | file_name | upload_time}"];
  SessionLog [label="{<session_id> session_id (PK) | <file_id> file_id (FK) | src_ip | dst_ip | protocol | start_time | end_time}"];
  FlowFeature [label="{<feature_id> feature_id (PK) | <session_id> session_id (FK) | feature_name | feature_value}"];
  BehaviorPattern [label="{<pattern_id> pattern_id (PK) | name | description}"];
  SessionPattern [label="{<session_id> session_id (FK) | <pattern_id> pattern_id (FK)}"];
  SimulationConfig [label="{<config_id> config_id (PK) | name | parameters}"];
  SimulationRun [label="{<run_id> run_id (PK) | <config_id> config_id (FK) | start_time | end_time | status}"];
  SimulationResult [label="{<result_id> result_id (PK) | <run_id> run_id (FK) | metric_name | metric_value}"];
  DomainMapping [label="{<domain_id> domain_id (PK) | domain | application_name}"];
  IPMapping [label="{<ip_id> ip_id (PK) | ip_address | application_name}"];

  PCAP_File:file_id -> SessionLog:file_id;
  SessionLog:session_id -> FlowFeature:session_id;
  SessionLog:session_id -> SessionPattern:session_id;
  BehaviorPattern:pattern_id -> SessionPattern:pattern_id;
  SimulationConfig:config_id -> SimulationRun:config_id;
  SimulationRun:run_id -> SimulationResult:run_id;
}
```