```mermaid
graph TD;
    A[核心交换机] --> B[服务器1(运行数据处理程序，负责对采集的数据进行清洗、分析等操作)];
    A --> C[服务器2(运行存储程序，用于存储原始数据和处理后的结果数据)];
    A --> D[Web服务器(运行Web服务程序，提供用户访问界面)];
    A --> E[防火墙(保障网络安全，过滤非法访问)];
    E --> F[外部网络];
    G[接入交换机1] --> H[工作站1(运行数据采集程序，收集业务数据)];
    G --> I[工作站2(运行数据采集程序，收集业务数据)];
    G --> A;
    J[接入交换机2] --> K[工作站3(运行数据采集程序，收集业务数据)];
    J --> L[工作站4(运行数据采集程序，收集业务数据)];
    J --> A;
  end
```